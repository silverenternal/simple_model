# Example to macro synthesis

Examples are structural before/after JSON values. The synthesizer infers typed
parameters and lossless edit operations, carries forbidden matches from negative
examples, and emits review-only candidates. A single example can never be
promoted to apply mode.
