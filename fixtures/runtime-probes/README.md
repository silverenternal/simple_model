# Runtime probe fixtures

Fixtures target named unresolved obligations with read-only argv commands. No
repository content is interpolated into shell strings; network is denied unless
a host appears in the explicit allowlist.
