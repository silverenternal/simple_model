# Dynamic Resolver SDK v2

Resolvers declare frameworks, versions, evidence inputs, output edge types, and
trust requirements. The harness records deterministic scoped provenance and
replays outputs offline. If two resolvers claim the same edge, the report is an
evidence conflict with no automatic winner.
