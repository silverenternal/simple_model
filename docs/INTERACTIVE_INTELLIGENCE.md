# Interactive intelligence cockpit

The terminal cockpit renders deterministic MQL plans, graph paths, evidence and
replay commands. Sessions export compact handoff bundles. Simulation remains
review-only when the worktree is dirty, certification is missing, or write intent
is absent; IDE/MCP projections cannot weaken the terminal policy.
