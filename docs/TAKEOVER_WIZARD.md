# Takeover wizard

The wizard creates a resumable, non-destructive session. Struct fragments,
parser/resolver configuration, interface commitments, owners, test mappings, and
the first optimization queue are proposed through a review-only macro plan.
Interface unblock consumes concrete parser/runtime/owner/contract/test evidence;
AI tasks are bounded domain-intent choices.
