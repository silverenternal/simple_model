# Symbol lifecycle macro pack

Lifecycle macros operate on stable symbol identities and update definitions,
references, imports, exports, docs, tests, and generated contracts. Reflection
and dynamic references require a trusted resolver; otherwise apply is blocked.
Deprecation emits a compatibility alias plus a removal deadline. Four macros
are certified apply-capable on the external fixture.
