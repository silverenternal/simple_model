# Local project intelligence daemon

simple_modeld is optional and local-only. Once mode snapshots files, graph/test/
drift partitions and policy state; it never applies macros. watch_invalidate
coalesces file events deterministically. A branch change marks stale evidence
for reset while preserving content-addressed facts.
