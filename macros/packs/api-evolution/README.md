# API evolution macro pack

Additive, rename, type-change, split/merge, pagination, error-model, and
authentication migrations emit compatibility matrices, consumer ledgers, and
generated client/server synchronization evidence.
