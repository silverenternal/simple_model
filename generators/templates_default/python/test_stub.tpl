"""Auto-generated test stubs for {{component_name}} (module: {{module_name}}).

每个 todo 都对应一条 test 函数，开发者把 `assert ...` 替换为真实断言。
"""
import pytest
from ..{{module_name}}.{{snake_name}} import {{component_name}}
{{todos_block}}

{{test_functions}}
